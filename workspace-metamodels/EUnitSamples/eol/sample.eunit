@setup
operation prepare() {
  var firstElement  := Element.allInstances.first();
}

@test
operation twoElements() {
  assertEquals(2, Element.allInstances.size());
}

@test
operation isolation1() {
  assertEquals('A', firstElement.name);
  firstElement.name := 'X';
}

@test
operation isolation2() {
  assertEquals('A', firstElement.name);
}
