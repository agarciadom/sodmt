$with Sequence {"", "A"}
@test
operation testForA() {
  assertTrue(Element.allInstances.exists(e|e.name = 'A'));
}

$with Sequence {"", "B"}
@test
operation testForA() {
  assertTrue(Element.allInstances.exists(e|e.name = 'C'));
}

$with Sequence {"", "A"}
$with Sequence {"", "B"}
@test
operation testForBoth() {
  assertTrue(Element.allInstances.size() > 0);
}
